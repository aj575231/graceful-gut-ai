fastapi==0.139.2
pydantic==2.13.4
mangum==0.21.0

# Secrets Manager retrieval for the shared secret, with per-container caching.
# boto3 is deliberately absent: the Python 3.13 Lambda runtime provides it, and
# Powertools imports it from there. See requirements.txt.
aws-lambda-powertools==3.31.1
